# Local environment files (may contain secrets)
host/.env
client/.env
**/.env
!**/.env.example

# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Claude Code local state
.claude/

# Python (Phase 3+ sidecar-loader)
__pycache__/
*.py[cod]
.venv/
venv/

# Logs
*.log

# Per-cycle sweep stdout dumps from run-overnight-screen.sh.
# These are raw `docker compose run ... | tee` traces — the analytical rollup
# lives in host/test/docs/difficulty-pack/explore/cN/summary.md. Keep
# pre-existing sprint-baseline reports (T32-TUNING-PROGRESS.md,
# TIER-EVAL-V2-SPRINT-1-BASELINE.md) which sit beside this pattern.
host/test/logs/OVERNIGHT-SCREEN-explore-*.md
